const config = {
    // 启动端口
    port: 5000,
  
    // 数据库配置
    database: {
      DATABASE: 'tests',
      USERNAME: 'root',
      PASSWORD: 'qqs290*',
      PORT: '3306',
      HOST: 'localhost'
    }
  }
  
  module.exports = config